
        <tbody>
            @foreach($comments as $comment)
                <tr class="hover:bg-gray-lighter">
                    <td class="py-4 px-6 border-b border-gray-light dark:border-gray-600">
                        <p class="text-left text-lg text-gray-dark dark:text-gray-200"></p>
                        <div class="flex">
                        <!--削除ボタン -->
                        </div>
                    </td>
                </tr>
            @endforeach
              
        </tbody>
 